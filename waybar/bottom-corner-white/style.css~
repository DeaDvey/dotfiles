/* -----------------------------------------------------
 * General 
 * ----------------------------------------------------- */
* {
	font-size: 16px;
	font-family: Ubuntu Nerd Font, Font Awesome, sans-serif;
    font-weight: bold;
    min-height: 30px;
    color: #ebdbb2;
}

window#waybar {
	background-color: rgba(235,219,178,0.4);
    border-bottom: 1px solid rgba(235,219,178,0);
    border-radius: 0px;
	color: #ebdbb2;
	margin: 20px;
}

window#waybar {
  border-radius: 0px 0px 15px 15px;
}

window#waybar {
  
  background-color: transparent;
  border-top-left-radius: 10px;
  box-shadow: 0 25px 0 0 rgba(235,219,178,1);
}


/* -----------------------------------------------------
 * Window
 * ----------------------------------------------------- */
#window {
    color: #ebdbb2;
    background: #c0caf5;
    border-radius: 15px;
    margin: 5px;
    padding: 2px 10px;
}

#clock {
    color: rgba(0,0,0,0);
    background: rgba(0,0,0,0);
    border-radius: 15px;
    margin: 5px;
    padding: 2px 10px;
}
